- clean: ['~']

- link:
    # Advanced format:
    # ~/.foo:
    #     path: foo
    #     create: false
    #     relink: false
    #     force: false

    ~/.gitconfig_local: git_local/gitconfig_local
    ~/.tmux_local: tmux_local/
    ~/.vimrc_local: vim_local
    ~/.zsh_local: zsh_local/

- shell:
    - [git submodule update --init --remote, Updating submodules]

